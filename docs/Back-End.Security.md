# Security

## CORS

- Only allows requests from `https://alexheffernan.dev`
- Only allows `POST` method and required headers

## Rate Limiting

- Configured to prevent abuse (e.g., 3 requests per 20 seconds)


## Recommendations

- Keep all dependencies up to date
- Monitor logs for suspicious activity
- Use strong, unique TLS certificates